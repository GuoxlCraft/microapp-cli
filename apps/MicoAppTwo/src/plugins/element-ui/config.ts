/**
 * 全局配置
 */
import { ConfigElement } from './types'

const elementConfig: ConfigElement = {
  /**
   * 尺寸
   */
  size: 'medium',

  /**
   * 层级
   */
  zIndex: 2000
}

export default elementConfig
